parameters:

services:
    _defaults:
        autowire: true
        autoconfigure: true

    App\:
        resource: '../src/App/'
        exclude:
            - '../src/App/Kernel.php'

    App\Http\Handler\:
        resource: '../src/Controller/'
        tags: ['controller.service_arguments']
